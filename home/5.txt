mdkir cyber
cd cyber
nano text.txt
(type anything and save )
sudo apt-get update
sudo apt-get install hping3 
python -- version
if version is 2 then:
python -m SimpleHTTPServer 80
if the version is 3:
python3 -m http.server 80
copy the link and paste in browser open the  text.txt in browser
sudo hping3 -S --flood -V -p 80 <target_IP>
target ip which  is showing in the terminal
go to another terminal tab and type
sudo tcpdump -i any port 80

